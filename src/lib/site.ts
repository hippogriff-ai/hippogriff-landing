export const SITE_URL = 'https://www.hippogriff.io';
export const SITE_NAME = 'Hippogriff';
export const AUTHOR = 'Vicki Zhang';

// Next.js replaces (not deep-merges) a page's `alternates` over the layout's,
// so every page that sets a canonical must re-declare the feed link.
export const FEED_ALTERNATE = {
  'application/rss+xml': '/feed.xml',
};
